name: flutter_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4  # Para la autenticación con Firebase
  google_sign_in: ^6.2.1 # Para la autenticación con Google
  flutter_facebook_auth: ^7.0.1 # Para la autenticación con Facebook
  provider: ^6.1.2       # Para la gestión de estado
  flutter_signin_button: ^2.0.0 # Para botones de inicio de sesión personalizados


dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^4.0.0


# The following section is specific to Flutter packages.
flutter:


  uses-material-design: true

  